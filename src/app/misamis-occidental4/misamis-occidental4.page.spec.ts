import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MisamisOccidental4Page } from './misamis-occidental4.page';

describe('MisamisOccidental4Page', () => {
  let component: MisamisOccidental4Page;
  let fixture: ComponentFixture<MisamisOccidental4Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(MisamisOccidental4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
