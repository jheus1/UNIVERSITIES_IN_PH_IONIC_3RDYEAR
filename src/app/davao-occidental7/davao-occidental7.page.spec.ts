import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoOccidental7Page } from './davao-occidental7.page';

describe('DavaoOccidental7Page', () => {
  let component: DavaoOccidental7Page;
  let fixture: ComponentFixture<DavaoOccidental7Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoOccidental7Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
