import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AgusanDelsur8Page } from './agusan-delsur8.page';

describe('AgusanDelsur8Page', () => {
  let component: AgusanDelsur8Page;
  let fixture: ComponentFixture<AgusanDelsur8Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(AgusanDelsur8Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
