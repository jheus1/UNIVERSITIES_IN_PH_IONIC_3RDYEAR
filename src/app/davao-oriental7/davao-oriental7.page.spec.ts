import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoOriental7Page } from './davao-oriental7.page';

describe('DavaoOriental7Page', () => {
  let component: DavaoOriental7Page;
  let fixture: ComponentFixture<DavaoOriental7Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoOriental7Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
