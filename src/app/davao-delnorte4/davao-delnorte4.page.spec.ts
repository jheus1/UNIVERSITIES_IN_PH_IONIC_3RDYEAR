import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoDelnorte4Page } from './davao-delnorte4.page';

describe('DavaoDelnorte4Page', () => {
  let component: DavaoDelnorte4Page;
  let fixture: ComponentFixture<DavaoDelnorte4Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoDelnorte4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
