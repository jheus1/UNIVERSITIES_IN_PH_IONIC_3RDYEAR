import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MisamisOccidental1Page } from './misamis-occidental1.page';

describe('MisamisOccidental1Page', () => {
  let component: MisamisOccidental1Page;
  let fixture: ComponentFixture<MisamisOccidental1Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(MisamisOccidental1Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
