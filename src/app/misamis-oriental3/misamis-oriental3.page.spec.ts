import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MisamisOriental3Page } from './misamis-oriental3.page';

describe('MisamisOriental3Page', () => {
  let component: MisamisOriental3Page;
  let fixture: ComponentFixture<MisamisOriental3Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(MisamisOriental3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
