import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DinagatIslands2Page } from './dinagat-islands2.page';

describe('DinagatIslands2Page', () => {
  let component: DinagatIslands2Page;
  let fixture: ComponentFixture<DinagatIslands2Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DinagatIslands2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
