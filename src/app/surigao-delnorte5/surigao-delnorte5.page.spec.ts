import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SurigaoDelnorte5Page } from './surigao-delnorte5.page';

describe('SurigaoDelnorte5Page', () => {
  let component: SurigaoDelnorte5Page;
  let fixture: ComponentFixture<SurigaoDelnorte5Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SurigaoDelnorte5Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
