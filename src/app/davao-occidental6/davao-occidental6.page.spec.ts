import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoOccidental6Page } from './davao-occidental6.page';

describe('DavaoOccidental6Page', () => {
  let component: DavaoOccidental6Page;
  let fixture: ComponentFixture<DavaoOccidental6Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoOccidental6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
