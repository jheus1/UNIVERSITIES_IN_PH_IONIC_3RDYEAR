import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DinagatIslands9Page } from './dinagat-islands9.page';

describe('DinagatIslands9Page', () => {
  let component: DinagatIslands9Page;
  let fixture: ComponentFixture<DinagatIslands9Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DinagatIslands9Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
