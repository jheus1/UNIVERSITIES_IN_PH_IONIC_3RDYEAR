import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MisamisOccidental6Page } from './misamis-occidental6.page';

describe('MisamisOccidental6Page', () => {
  let component: MisamisOccidental6Page;
  let fixture: ComponentFixture<MisamisOccidental6Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(MisamisOccidental6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
