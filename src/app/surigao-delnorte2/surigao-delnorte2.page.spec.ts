import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SurigaoDelnorte2Page } from './surigao-delnorte2.page';

describe('SurigaoDelnorte2Page', () => {
  let component: SurigaoDelnorte2Page;
  let fixture: ComponentFixture<SurigaoDelnorte2Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SurigaoDelnorte2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
