import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ZamboangaDelnorte8Page } from './zamboanga-delnorte8.page';

describe('ZamboangaDelnorte8Page', () => {
  let component: ZamboangaDelnorte8Page;
  let fixture: ComponentFixture<ZamboangaDelnorte8Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ZamboangaDelnorte8Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
