import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MisamisOriental6Page } from './misamis-oriental6.page';

describe('MisamisOriental6Page', () => {
  let component: MisamisOriental6Page;
  let fixture: ComponentFixture<MisamisOriental6Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(MisamisOriental6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
