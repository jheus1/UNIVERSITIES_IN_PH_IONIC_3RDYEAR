import { ComponentFixture, TestBed } from '@angular/core/testing';
import { LanaoDelnorte4Page } from './lanao-delnorte4.page';

describe('LanaoDelnorte4Page', () => {
  let component: LanaoDelnorte4Page;
  let fixture: ComponentFixture<LanaoDelnorte4Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(LanaoDelnorte4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
