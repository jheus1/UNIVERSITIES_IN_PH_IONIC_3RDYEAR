import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MisamisOccidental5Page } from './misamis-occidental5.page';

describe('MisamisOccidental5Page', () => {
  let component: MisamisOccidental5Page;
  let fixture: ComponentFixture<MisamisOccidental5Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(MisamisOccidental5Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
