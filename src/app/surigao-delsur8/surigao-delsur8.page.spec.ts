import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SurigaoDelsur8Page } from './surigao-delsur8.page';

describe('SurigaoDelsur8Page', () => {
  let component: SurigaoDelsur8Page;
  let fixture: ComponentFixture<SurigaoDelsur8Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SurigaoDelsur8Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
