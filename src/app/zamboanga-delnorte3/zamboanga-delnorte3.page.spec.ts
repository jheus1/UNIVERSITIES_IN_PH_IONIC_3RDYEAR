import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ZamboangaDelnorte3Page } from './zamboanga-delnorte3.page';

describe('ZamboangaDelnorte3Page', () => {
  let component: ZamboangaDelnorte3Page;
  let fixture: ComponentFixture<ZamboangaDelnorte3Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ZamboangaDelnorte3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
