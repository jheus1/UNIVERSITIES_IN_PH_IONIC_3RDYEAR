import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MisamisOccidental3Page } from './misamis-occidental3.page';

describe('MisamisOccidental3Page', () => {
  let component: MisamisOccidental3Page;
  let fixture: ComponentFixture<MisamisOccidental3Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(MisamisOccidental3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
