import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ZamboangaDelnorte10Page } from './zamboanga-delnorte10.page';

describe('ZamboangaDelnorte10Page', () => {
  let component: ZamboangaDelnorte10Page;
  let fixture: ComponentFixture<ZamboangaDelnorte10Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ZamboangaDelnorte10Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
