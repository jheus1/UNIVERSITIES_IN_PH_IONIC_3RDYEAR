import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AgusanDelsur1Page } from './agusan-delsur1.page';

describe('AgusanDelsur1Page', () => {
  let component: AgusanDelsur1Page;
  let fixture: ComponentFixture<AgusanDelsur1Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(AgusanDelsur1Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
