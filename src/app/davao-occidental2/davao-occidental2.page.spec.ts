import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoOccidental2Page } from './davao-occidental2.page';

describe('DavaoOccidental2Page', () => {
  let component: DavaoOccidental2Page;
  let fixture: ComponentFixture<DavaoOccidental2Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoOccidental2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
