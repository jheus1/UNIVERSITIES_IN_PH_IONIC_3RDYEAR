import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SurigaoDelnorte8Page } from './surigao-delnorte8.page';

describe('SurigaoDelnorte8Page', () => {
  let component: SurigaoDelnorte8Page;
  let fixture: ComponentFixture<SurigaoDelnorte8Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SurigaoDelnorte8Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
