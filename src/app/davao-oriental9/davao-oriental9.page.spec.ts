import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoOriental9Page } from './davao-oriental9.page';

describe('DavaoOriental9Page', () => {
  let component: DavaoOriental9Page;
  let fixture: ComponentFixture<DavaoOriental9Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoOriental9Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
