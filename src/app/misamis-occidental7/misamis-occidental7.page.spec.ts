import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MisamisOccidental7Page } from './misamis-occidental7.page';

describe('MisamisOccidental7Page', () => {
  let component: MisamisOccidental7Page;
  let fixture: ComponentFixture<MisamisOccidental7Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(MisamisOccidental7Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
