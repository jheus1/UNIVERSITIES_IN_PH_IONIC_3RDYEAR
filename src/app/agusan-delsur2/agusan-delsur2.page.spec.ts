import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AgusanDelsur2Page } from './agusan-delsur2.page';

describe('AgusanDelsur2Page', () => {
  let component: AgusanDelsur2Page;
  let fixture: ComponentFixture<AgusanDelsur2Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(AgusanDelsur2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
