import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SurigaoDelsur4Page } from './surigao-delsur4.page';

describe('SurigaoDelsur4Page', () => {
  let component: SurigaoDelsur4Page;
  let fixture: ComponentFixture<SurigaoDelsur4Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SurigaoDelsur4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
