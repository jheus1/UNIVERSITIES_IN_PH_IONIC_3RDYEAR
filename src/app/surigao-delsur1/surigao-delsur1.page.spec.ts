import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SurigaoDelsur1Page } from './surigao-delsur1.page';

describe('SurigaoDelsur1Page', () => {
  let component: SurigaoDelsur1Page;
  let fixture: ComponentFixture<SurigaoDelsur1Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SurigaoDelsur1Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
