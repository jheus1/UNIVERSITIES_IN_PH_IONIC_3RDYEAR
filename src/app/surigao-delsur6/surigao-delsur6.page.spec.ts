import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SurigaoDelsur6Page } from './surigao-delsur6.page';

describe('SurigaoDelsur6Page', () => {
  let component: SurigaoDelsur6Page;
  let fixture: ComponentFixture<SurigaoDelsur6Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SurigaoDelsur6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
