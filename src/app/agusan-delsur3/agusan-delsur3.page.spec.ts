import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AgusanDelsur3Page } from './agusan-delsur3.page';

describe('AgusanDelsur3Page', () => {
  let component: AgusanDelsur3Page;
  let fixture: ComponentFixture<AgusanDelsur3Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(AgusanDelsur3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
