import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SultanKudarat2Page } from './sultan-kudarat2.page';

describe('SultanKudarat2Page', () => {
  let component: SultanKudarat2Page;
  let fixture: ComponentFixture<SultanKudarat2Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SultanKudarat2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
