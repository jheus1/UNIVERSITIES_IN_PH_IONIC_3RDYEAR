import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoOriental6Page } from './davao-oriental6.page';

describe('DavaoOriental6Page', () => {
  let component: DavaoOriental6Page;
  let fixture: ComponentFixture<DavaoOriental6Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoOriental6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
