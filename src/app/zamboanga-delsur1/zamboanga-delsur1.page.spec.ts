import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ZamboangaDelsur1Page } from './zamboanga-delsur1.page';

describe('ZamboangaDelsur1Page', () => {
  let component: ZamboangaDelsur1Page;
  let fixture: ComponentFixture<ZamboangaDelsur1Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ZamboangaDelsur1Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
