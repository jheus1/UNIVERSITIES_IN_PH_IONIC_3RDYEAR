import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ZamboangaDelnorte7Page } from './zamboanga-delnorte7.page';

describe('ZamboangaDelnorte7Page', () => {
  let component: ZamboangaDelnorte7Page;
  let fixture: ComponentFixture<ZamboangaDelnorte7Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ZamboangaDelnorte7Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
