import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SurigaoDelnorte3Page } from './surigao-delnorte3.page';

describe('SurigaoDelnorte3Page', () => {
  let component: SurigaoDelnorte3Page;
  let fixture: ComponentFixture<SurigaoDelnorte3Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SurigaoDelnorte3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
