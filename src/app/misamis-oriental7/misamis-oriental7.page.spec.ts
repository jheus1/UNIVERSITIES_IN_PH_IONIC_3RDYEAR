import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MisamisOriental7Page } from './misamis-oriental7.page';

describe('MisamisOriental7Page', () => {
  let component: MisamisOriental7Page;
  let fixture: ComponentFixture<MisamisOriental7Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(MisamisOriental7Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
