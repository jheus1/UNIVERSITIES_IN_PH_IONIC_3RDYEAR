import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MisamisOriental10Page } from './misamis-oriental10.page';

describe('MisamisOriental10Page', () => {
  let component: MisamisOriental10Page;
  let fixture: ComponentFixture<MisamisOriental10Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(MisamisOriental10Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
