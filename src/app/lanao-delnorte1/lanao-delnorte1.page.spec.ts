import { ComponentFixture, TestBed } from '@angular/core/testing';
import { LanaoDelnorte1Page } from './lanao-delnorte1.page';

describe('LanaoDelnorte1Page', () => {
  let component: LanaoDelnorte1Page;
  let fixture: ComponentFixture<LanaoDelnorte1Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(LanaoDelnorte1Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
