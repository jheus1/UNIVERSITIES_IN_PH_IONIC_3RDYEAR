import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ZamboangaDelsur2Page } from './zamboanga-delsur2.page';

describe('ZamboangaDelsur2Page', () => {
  let component: ZamboangaDelsur2Page;
  let fixture: ComponentFixture<ZamboangaDelsur2Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ZamboangaDelsur2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
