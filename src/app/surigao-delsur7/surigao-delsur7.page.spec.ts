import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SurigaoDelsur7Page } from './surigao-delsur7.page';

describe('SurigaoDelsur7Page', () => {
  let component: SurigaoDelsur7Page;
  let fixture: ComponentFixture<SurigaoDelsur7Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SurigaoDelsur7Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
