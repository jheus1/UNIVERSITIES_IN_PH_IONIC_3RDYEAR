import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoOriental5Page } from './davao-oriental5.page';

describe('DavaoOriental5Page', () => {
  let component: DavaoOriental5Page;
  let fixture: ComponentFixture<DavaoOriental5Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoOriental5Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
