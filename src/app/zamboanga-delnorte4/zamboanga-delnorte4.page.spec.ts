import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ZamboangaDelnorte4Page } from './zamboanga-delnorte4.page';

describe('ZamboangaDelnorte4Page', () => {
  let component: ZamboangaDelnorte4Page;
  let fixture: ComponentFixture<ZamboangaDelnorte4Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ZamboangaDelnorte4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
