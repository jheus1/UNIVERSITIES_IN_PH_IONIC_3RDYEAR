import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoOriental3Page } from './davao-oriental3.page';

describe('DavaoOriental3Page', () => {
  let component: DavaoOriental3Page;
  let fixture: ComponentFixture<DavaoOriental3Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoOriental3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
