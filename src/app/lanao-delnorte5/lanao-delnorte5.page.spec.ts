import { ComponentFixture, TestBed } from '@angular/core/testing';
import { LanaoDelnorte5Page } from './lanao-delnorte5.page';

describe('LanaoDelnorte5Page', () => {
  let component: LanaoDelnorte5Page;
  let fixture: ComponentFixture<LanaoDelnorte5Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(LanaoDelnorte5Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
