import { ComponentFixture, TestBed } from '@angular/core/testing';
import { LanaoDelnorte8Page } from './lanao-delnorte8.page';

describe('LanaoDelnorte8Page', () => {
  let component: LanaoDelnorte8Page;
  let fixture: ComponentFixture<LanaoDelnorte8Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(LanaoDelnorte8Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
