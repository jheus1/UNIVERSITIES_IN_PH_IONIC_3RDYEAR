import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoOccidental1Page } from './davao-occidental1.page';

describe('DavaoOccidental1Page', () => {
  let component: DavaoOccidental1Page;
  let fixture: ComponentFixture<DavaoOccidental1Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoOccidental1Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
