import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MisamisOriental8Page } from './misamis-oriental8.page';

describe('MisamisOriental8Page', () => {
  let component: MisamisOriental8Page;
  let fixture: ComponentFixture<MisamisOriental8Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(MisamisOriental8Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
