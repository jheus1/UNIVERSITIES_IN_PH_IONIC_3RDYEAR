import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AgusanDelnorte1Page } from './agusan-delnorte1.page';

describe('AgusanDelnorte1Page', () => {
  let component: AgusanDelnorte1Page;
  let fixture: ComponentFixture<AgusanDelnorte1Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(AgusanDelnorte1Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
