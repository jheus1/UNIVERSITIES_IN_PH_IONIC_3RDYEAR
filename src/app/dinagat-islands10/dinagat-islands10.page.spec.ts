import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DinagatIslands10Page } from './dinagat-islands10.page';

describe('DinagatIslands10Page', () => {
  let component: DinagatIslands10Page;
  let fixture: ComponentFixture<DinagatIslands10Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DinagatIslands10Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
