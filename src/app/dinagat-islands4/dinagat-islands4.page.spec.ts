import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DinagatIslands4Page } from './dinagat-islands4.page';

describe('DinagatIslands4Page', () => {
  let component: DinagatIslands4Page;
  let fixture: ComponentFixture<DinagatIslands4Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DinagatIslands4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
