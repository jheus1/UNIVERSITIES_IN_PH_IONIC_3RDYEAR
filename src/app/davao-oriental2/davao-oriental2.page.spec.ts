import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoOriental2Page } from './davao-oriental2.page';

describe('DavaoOriental2Page', () => {
  let component: DavaoOriental2Page;
  let fixture: ComponentFixture<DavaoOriental2Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoOriental2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
