import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SurigaoDelsur5Page } from './surigao-delsur5.page';

describe('SurigaoDelsur5Page', () => {
  let component: SurigaoDelsur5Page;
  let fixture: ComponentFixture<SurigaoDelsur5Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SurigaoDelsur5Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
