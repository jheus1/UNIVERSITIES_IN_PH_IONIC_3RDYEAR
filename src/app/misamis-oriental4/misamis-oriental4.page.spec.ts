import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MisamisOriental4Page } from './misamis-oriental4.page';

describe('MisamisOriental4Page', () => {
  let component: MisamisOriental4Page;
  let fixture: ComponentFixture<MisamisOriental4Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(MisamisOriental4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
