import { ComponentFixture, TestBed } from '@angular/core/testing';
import { LanaoDelnorte9Page } from './lanao-delnorte9.page';

describe('LanaoDelnorte9Page', () => {
  let component: LanaoDelnorte9Page;
  let fixture: ComponentFixture<LanaoDelnorte9Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(LanaoDelnorte9Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
