import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ZamboangaDelnorte1Page } from './zamboanga-delnorte1.page';

describe('ZamboangaDelnorte1Page', () => {
  let component: ZamboangaDelnorte1Page;
  let fixture: ComponentFixture<ZamboangaDelnorte1Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ZamboangaDelnorte1Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
