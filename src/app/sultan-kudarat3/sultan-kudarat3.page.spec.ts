import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SultanKudarat3Page } from './sultan-kudarat3.page';

describe('SultanKudarat3Page', () => {
  let component: SultanKudarat3Page;
  let fixture: ComponentFixture<SultanKudarat3Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SultanKudarat3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
