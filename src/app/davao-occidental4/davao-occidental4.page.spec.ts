import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoOccidental4Page } from './davao-occidental4.page';

describe('DavaoOccidental4Page', () => {
  let component: DavaoOccidental4Page;
  let fixture: ComponentFixture<DavaoOccidental4Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoOccidental4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
