import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AgusanDelsur5Page } from './agusan-delsur5.page';

describe('AgusanDelsur5Page', () => {
  let component: AgusanDelsur5Page;
  let fixture: ComponentFixture<AgusanDelsur5Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(AgusanDelsur5Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
