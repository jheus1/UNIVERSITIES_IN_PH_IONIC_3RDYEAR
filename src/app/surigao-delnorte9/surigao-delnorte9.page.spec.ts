import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SurigaoDelnorte9Page } from './surigao-delnorte9.page';

describe('SurigaoDelnorte9Page', () => {
  let component: SurigaoDelnorte9Page;
  let fixture: ComponentFixture<SurigaoDelnorte9Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SurigaoDelnorte9Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
