import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DinagatIslands5Page } from './dinagat-islands5.page';

describe('DinagatIslands5Page', () => {
  let component: DinagatIslands5Page;
  let fixture: ComponentFixture<DinagatIslands5Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DinagatIslands5Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
