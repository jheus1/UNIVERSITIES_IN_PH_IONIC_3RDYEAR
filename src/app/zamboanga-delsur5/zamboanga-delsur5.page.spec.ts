import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ZamboangaDelsur5Page } from './zamboanga-delsur5.page';

describe('ZamboangaDelsur5Page', () => {
  let component: ZamboangaDelsur5Page;
  let fixture: ComponentFixture<ZamboangaDelsur5Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ZamboangaDelsur5Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
