import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SultanKudarat6Page } from './sultan-kudarat6.page';

describe('SultanKudarat6Page', () => {
  let component: SultanKudarat6Page;
  let fixture: ComponentFixture<SultanKudarat6Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SultanKudarat6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
