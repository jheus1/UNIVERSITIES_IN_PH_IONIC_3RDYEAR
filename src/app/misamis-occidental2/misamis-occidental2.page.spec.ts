import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MisamisOccidental2Page } from './misamis-occidental2.page';

describe('MisamisOccidental2Page', () => {
  let component: MisamisOccidental2Page;
  let fixture: ComponentFixture<MisamisOccidental2Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(MisamisOccidental2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
