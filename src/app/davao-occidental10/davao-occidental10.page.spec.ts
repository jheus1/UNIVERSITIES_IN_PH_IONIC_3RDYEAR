import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoOccidental10Page } from './davao-occidental10.page';

describe('DavaoOccidental10Page', () => {
  let component: DavaoOccidental10Page;
  let fixture: ComponentFixture<DavaoOccidental10Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoOccidental10Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
