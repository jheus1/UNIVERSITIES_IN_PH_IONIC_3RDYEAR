import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ZamboangaDelsur6Page } from './zamboanga-delsur6.page';

describe('ZamboangaDelsur6Page', () => {
  let component: ZamboangaDelsur6Page;
  let fixture: ComponentFixture<ZamboangaDelsur6Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ZamboangaDelsur6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
