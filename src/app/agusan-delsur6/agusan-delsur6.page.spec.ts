import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AgusanDelsur6Page } from './agusan-delsur6.page';

describe('AgusanDelsur6Page', () => {
  let component: AgusanDelsur6Page;
  let fixture: ComponentFixture<AgusanDelsur6Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(AgusanDelsur6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
