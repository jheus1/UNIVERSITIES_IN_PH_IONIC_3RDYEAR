import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoDelnorte3Page } from './davao-delnorte3.page';

describe('DavaoDelnorte3Page', () => {
  let component: DavaoDelnorte3Page;
  let fixture: ComponentFixture<DavaoDelnorte3Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoDelnorte3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
