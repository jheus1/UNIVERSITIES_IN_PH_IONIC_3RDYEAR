import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoOriental4Page } from './davao-oriental4.page';

describe('DavaoOriental4Page', () => {
  let component: DavaoOriental4Page;
  let fixture: ComponentFixture<DavaoOriental4Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoOriental4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
