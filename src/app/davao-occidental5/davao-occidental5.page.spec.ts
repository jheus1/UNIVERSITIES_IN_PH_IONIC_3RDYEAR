import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoOccidental5Page } from './davao-occidental5.page';

describe('DavaoOccidental5Page', () => {
  let component: DavaoOccidental5Page;
  let fixture: ComponentFixture<DavaoOccidental5Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoOccidental5Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
