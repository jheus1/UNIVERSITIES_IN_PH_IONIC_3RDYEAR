import { ComponentFixture, TestBed } from '@angular/core/testing';
import { LanaoDelnorte3Page } from './lanao-delnorte3.page';

describe('LanaoDelnorte3Page', () => {
  let component: LanaoDelnorte3Page;
  let fixture: ComponentFixture<LanaoDelnorte3Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(LanaoDelnorte3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
