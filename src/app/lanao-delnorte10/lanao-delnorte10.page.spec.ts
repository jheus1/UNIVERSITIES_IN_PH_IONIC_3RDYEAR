import { ComponentFixture, TestBed } from '@angular/core/testing';
import { LanaoDelnorte10Page } from './lanao-delnorte10.page';

describe('LanaoDelnorte10Page', () => {
  let component: LanaoDelnorte10Page;
  let fixture: ComponentFixture<LanaoDelnorte10Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(LanaoDelnorte10Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
