import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AgusanDelsur4Page } from './agusan-delsur4.page';

describe('AgusanDelsur4Page', () => {
  let component: AgusanDelsur4Page;
  let fixture: ComponentFixture<AgusanDelsur4Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(AgusanDelsur4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
