import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SurigaoDelsur9Page } from './surigao-delsur9.page';

describe('SurigaoDelsur9Page', () => {
  let component: SurigaoDelsur9Page;
  let fixture: ComponentFixture<SurigaoDelsur9Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SurigaoDelsur9Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
