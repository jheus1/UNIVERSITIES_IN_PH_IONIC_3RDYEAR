import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SultanKudarat8Page } from './sultan-kudarat8.page';

describe('SultanKudarat8Page', () => {
  let component: SultanKudarat8Page;
  let fixture: ComponentFixture<SultanKudarat8Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SultanKudarat8Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
