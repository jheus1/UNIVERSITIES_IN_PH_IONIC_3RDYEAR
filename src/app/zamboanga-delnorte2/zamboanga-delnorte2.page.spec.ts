import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ZamboangaDelnorte2Page } from './zamboanga-delnorte2.page';

describe('ZamboangaDelnorte2Page', () => {
  let component: ZamboangaDelnorte2Page;
  let fixture: ComponentFixture<ZamboangaDelnorte2Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ZamboangaDelnorte2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
