import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoOriental8Page } from './davao-oriental8.page';

describe('DavaoOriental8Page', () => {
  let component: DavaoOriental8Page;
  let fixture: ComponentFixture<DavaoOriental8Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoOriental8Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
