import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AgusanDelsur10Page } from './agusan-delsur10.page';

describe('AgusanDelsur10Page', () => {
  let component: AgusanDelsur10Page;
  let fixture: ComponentFixture<AgusanDelsur10Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(AgusanDelsur10Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
