import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MisamisOccidental10Page } from './misamis-occidental10.page';

describe('MisamisOccidental10Page', () => {
  let component: MisamisOccidental10Page;
  let fixture: ComponentFixture<MisamisOccidental10Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(MisamisOccidental10Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
