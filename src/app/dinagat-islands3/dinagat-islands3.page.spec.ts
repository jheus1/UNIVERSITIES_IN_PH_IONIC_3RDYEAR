import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DinagatIslands3Page } from './dinagat-islands3.page';

describe('DinagatIslands3Page', () => {
  let component: DinagatIslands3Page;
  let fixture: ComponentFixture<DinagatIslands3Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DinagatIslands3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
