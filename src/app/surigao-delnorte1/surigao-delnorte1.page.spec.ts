import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SurigaoDelnorte1Page } from './surigao-delnorte1.page';

describe('SurigaoDelnorte1Page', () => {
  let component: SurigaoDelnorte1Page;
  let fixture: ComponentFixture<SurigaoDelnorte1Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SurigaoDelnorte1Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
