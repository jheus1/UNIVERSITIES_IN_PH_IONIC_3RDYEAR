import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ZamboangaDelnorte5Page } from './zamboanga-delnorte5.page';

describe('ZamboangaDelnorte5Page', () => {
  let component: ZamboangaDelnorte5Page;
  let fixture: ComponentFixture<ZamboangaDelnorte5Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ZamboangaDelnorte5Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
