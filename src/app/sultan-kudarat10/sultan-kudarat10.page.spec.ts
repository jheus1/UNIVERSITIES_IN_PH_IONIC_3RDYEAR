import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SultanKudarat10Page } from './sultan-kudarat10.page';

describe('SultanKudarat10Page', () => {
  let component: SultanKudarat10Page;
  let fixture: ComponentFixture<SultanKudarat10Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SultanKudarat10Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
