import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SultanKudarat5Page } from './sultan-kudarat5.page';

describe('SultanKudarat5Page', () => {
  let component: SultanKudarat5Page;
  let fixture: ComponentFixture<SultanKudarat5Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SultanKudarat5Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
