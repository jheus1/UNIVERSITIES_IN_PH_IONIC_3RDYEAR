import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SultanKudarat9Page } from './sultan-kudarat9.page';

describe('SultanKudarat9Page', () => {
  let component: SultanKudarat9Page;
  let fixture: ComponentFixture<SultanKudarat9Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SultanKudarat9Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
