import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SurigaoDelsur2Page } from './surigao-delsur2.page';

describe('SurigaoDelsur2Page', () => {
  let component: SurigaoDelsur2Page;
  let fixture: ComponentFixture<SurigaoDelsur2Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SurigaoDelsur2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
