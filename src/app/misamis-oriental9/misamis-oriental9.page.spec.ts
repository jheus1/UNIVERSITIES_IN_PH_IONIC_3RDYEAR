import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MisamisOriental9Page } from './misamis-oriental9.page';

describe('MisamisOriental9Page', () => {
  let component: MisamisOriental9Page;
  let fixture: ComponentFixture<MisamisOriental9Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(MisamisOriental9Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
