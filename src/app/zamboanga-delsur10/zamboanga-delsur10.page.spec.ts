import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ZamboangaDelsur10Page } from './zamboanga-delsur10.page';

describe('ZamboangaDelsur10Page', () => {
  let component: ZamboangaDelsur10Page;
  let fixture: ComponentFixture<ZamboangaDelsur10Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ZamboangaDelsur10Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
