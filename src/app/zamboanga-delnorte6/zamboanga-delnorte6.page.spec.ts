import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ZamboangaDelnorte6Page } from './zamboanga-delnorte6.page';

describe('ZamboangaDelnorte6Page', () => {
  let component: ZamboangaDelnorte6Page;
  let fixture: ComponentFixture<ZamboangaDelnorte6Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ZamboangaDelnorte6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
