import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SurigaoDelnorte10Page } from './surigao-delnorte10.page';

describe('SurigaoDelnorte10Page', () => {
  let component: SurigaoDelnorte10Page;
  let fixture: ComponentFixture<SurigaoDelnorte10Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SurigaoDelnorte10Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
