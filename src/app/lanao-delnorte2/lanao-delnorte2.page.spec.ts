import { ComponentFixture, TestBed } from '@angular/core/testing';
import { LanaoDelnorte2Page } from './lanao-delnorte2.page';

describe('LanaoDelnorte2Page', () => {
  let component: LanaoDelnorte2Page;
  let fixture: ComponentFixture<LanaoDelnorte2Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(LanaoDelnorte2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
