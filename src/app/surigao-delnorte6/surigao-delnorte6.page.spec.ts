import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SurigaoDelnorte6Page } from './surigao-delnorte6.page';

describe('SurigaoDelnorte6Page', () => {
  let component: SurigaoDelnorte6Page;
  let fixture: ComponentFixture<SurigaoDelnorte6Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SurigaoDelnorte6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
