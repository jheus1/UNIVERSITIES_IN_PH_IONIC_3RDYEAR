import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ZamboangaDelsur3Page } from './zamboanga-delsur3.page';

describe('ZamboangaDelsur3Page', () => {
  let component: ZamboangaDelsur3Page;
  let fixture: ComponentFixture<ZamboangaDelsur3Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ZamboangaDelsur3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
