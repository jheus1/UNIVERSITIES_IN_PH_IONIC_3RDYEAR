import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SurigaoDelsur10Page } from './surigao-delsur10.page';

describe('SurigaoDelsur10Page', () => {
  let component: SurigaoDelsur10Page;
  let fixture: ComponentFixture<SurigaoDelsur10Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SurigaoDelsur10Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
