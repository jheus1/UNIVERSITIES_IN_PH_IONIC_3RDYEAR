import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SultanKudarat7Page } from './sultan-kudarat7.page';

describe('SultanKudarat7Page', () => {
  let component: SultanKudarat7Page;
  let fixture: ComponentFixture<SultanKudarat7Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SultanKudarat7Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
