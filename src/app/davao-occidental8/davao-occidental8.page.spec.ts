import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoOccidental8Page } from './davao-occidental8.page';

describe('DavaoOccidental8Page', () => {
  let component: DavaoOccidental8Page;
  let fixture: ComponentFixture<DavaoOccidental8Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoOccidental8Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
