import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoOriental10Page } from './davao-oriental10.page';

describe('DavaoOriental10Page', () => {
  let component: DavaoOriental10Page;
  let fixture: ComponentFixture<DavaoOriental10Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoOriental10Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
