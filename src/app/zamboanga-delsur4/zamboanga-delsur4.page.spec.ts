import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ZamboangaDelsur4Page } from './zamboanga-delsur4.page';

describe('ZamboangaDelsur4Page', () => {
  let component: ZamboangaDelsur4Page;
  let fixture: ComponentFixture<ZamboangaDelsur4Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ZamboangaDelsur4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
