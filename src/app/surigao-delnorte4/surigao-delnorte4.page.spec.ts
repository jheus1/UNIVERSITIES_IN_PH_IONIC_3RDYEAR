import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SurigaoDelnorte4Page } from './surigao-delnorte4.page';

describe('SurigaoDelnorte4Page', () => {
  let component: SurigaoDelnorte4Page;
  let fixture: ComponentFixture<SurigaoDelnorte4Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SurigaoDelnorte4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
