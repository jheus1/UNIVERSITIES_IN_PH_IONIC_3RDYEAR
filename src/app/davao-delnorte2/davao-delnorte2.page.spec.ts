import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoDelnorte2Page } from './davao-delnorte2.page';

describe('DavaoDelnorte2Page', () => {
  let component: DavaoDelnorte2Page;
  let fixture: ComponentFixture<DavaoDelnorte2Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoDelnorte2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
