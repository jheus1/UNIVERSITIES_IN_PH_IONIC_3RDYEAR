import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SultanKudarat4Page } from './sultan-kudarat4.page';

describe('SultanKudarat4Page', () => {
  let component: SultanKudarat4Page;
  let fixture: ComponentFixture<SultanKudarat4Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SultanKudarat4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
