import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoOccidental9Page } from './davao-occidental9.page';

describe('DavaoOccidental9Page', () => {
  let component: DavaoOccidental9Page;
  let fixture: ComponentFixture<DavaoOccidental9Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoOccidental9Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
