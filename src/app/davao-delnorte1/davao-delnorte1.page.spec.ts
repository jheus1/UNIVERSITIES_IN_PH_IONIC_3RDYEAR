import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoDelnorte1Page } from './davao-delnorte1.page';

describe('DavaoDelnorte1Page', () => {
  let component: DavaoDelnorte1Page;
  let fixture: ComponentFixture<DavaoDelnorte1Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoDelnorte1Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
