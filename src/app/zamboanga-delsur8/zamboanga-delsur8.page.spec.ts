import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ZamboangaDelsur8Page } from './zamboanga-delsur8.page';

describe('ZamboangaDelsur8Page', () => {
  let component: ZamboangaDelsur8Page;
  let fixture: ComponentFixture<ZamboangaDelsur8Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ZamboangaDelsur8Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
