import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SultanKudarat1Page } from './sultan-kudarat1.page';

describe('SultanKudarat1Page', () => {
  let component: SultanKudarat1Page;
  let fixture: ComponentFixture<SultanKudarat1Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SultanKudarat1Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
