import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DavaoOccidental3Page } from './davao-occidental3.page';

describe('DavaoOccidental3Page', () => {
  let component: DavaoOccidental3Page;
  let fixture: ComponentFixture<DavaoOccidental3Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DavaoOccidental3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
