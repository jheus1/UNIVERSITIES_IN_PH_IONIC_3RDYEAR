import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AgusanDelsur9Page } from './agusan-delsur9.page';

describe('AgusanDelsur9Page', () => {
  let component: AgusanDelsur9Page;
  let fixture: ComponentFixture<AgusanDelsur9Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(AgusanDelsur9Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
