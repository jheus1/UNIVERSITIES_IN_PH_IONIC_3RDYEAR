import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MisamisOccidental8Page } from './misamis-occidental8.page';

describe('MisamisOccidental8Page', () => {
  let component: MisamisOccidental8Page;
  let fixture: ComponentFixture<MisamisOccidental8Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(MisamisOccidental8Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
