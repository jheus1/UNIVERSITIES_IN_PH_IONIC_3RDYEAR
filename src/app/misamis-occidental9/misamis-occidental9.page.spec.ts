import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MisamisOccidental9Page } from './misamis-occidental9.page';

describe('MisamisOccidental9Page', () => {
  let component: MisamisOccidental9Page;
  let fixture: ComponentFixture<MisamisOccidental9Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(MisamisOccidental9Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
