import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ZamboangaDelnorte9Page } from './zamboanga-delnorte9.page';

describe('ZamboangaDelnorte9Page', () => {
  let component: ZamboangaDelnorte9Page;
  let fixture: ComponentFixture<ZamboangaDelnorte9Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ZamboangaDelnorte9Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
