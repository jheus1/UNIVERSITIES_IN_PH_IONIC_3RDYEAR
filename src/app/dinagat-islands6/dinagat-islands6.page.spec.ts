import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DinagatIslands6Page } from './dinagat-islands6.page';

describe('DinagatIslands6Page', () => {
  let component: DinagatIslands6Page;
  let fixture: ComponentFixture<DinagatIslands6Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DinagatIslands6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
