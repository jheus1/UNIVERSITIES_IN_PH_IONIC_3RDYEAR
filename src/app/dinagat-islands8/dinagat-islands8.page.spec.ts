import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DinagatIslands8Page } from './dinagat-islands8.page';

describe('DinagatIslands8Page', () => {
  let component: DinagatIslands8Page;
  let fixture: ComponentFixture<DinagatIslands8Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DinagatIslands8Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
