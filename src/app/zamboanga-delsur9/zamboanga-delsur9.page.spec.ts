import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ZamboangaDelsur9Page } from './zamboanga-delsur9.page';

describe('ZamboangaDelsur9Page', () => {
  let component: ZamboangaDelsur9Page;
  let fixture: ComponentFixture<ZamboangaDelsur9Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ZamboangaDelsur9Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
