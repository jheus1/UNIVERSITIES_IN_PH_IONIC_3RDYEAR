import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DinagatIslands7Page } from './dinagat-islands7.page';

describe('DinagatIslands7Page', () => {
  let component: DinagatIslands7Page;
  let fixture: ComponentFixture<DinagatIslands7Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(DinagatIslands7Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
