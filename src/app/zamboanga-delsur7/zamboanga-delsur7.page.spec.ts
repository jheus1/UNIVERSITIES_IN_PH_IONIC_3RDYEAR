import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ZamboangaDelsur7Page } from './zamboanga-delsur7.page';

describe('ZamboangaDelsur7Page', () => {
  let component: ZamboangaDelsur7Page;
  let fixture: ComponentFixture<ZamboangaDelsur7Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ZamboangaDelsur7Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
