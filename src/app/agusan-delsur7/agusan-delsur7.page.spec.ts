import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AgusanDelsur7Page } from './agusan-delsur7.page';

describe('AgusanDelsur7Page', () => {
  let component: AgusanDelsur7Page;
  let fixture: ComponentFixture<AgusanDelsur7Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(AgusanDelsur7Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
