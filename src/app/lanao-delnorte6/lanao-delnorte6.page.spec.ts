import { ComponentFixture, TestBed } from '@angular/core/testing';
import { LanaoDelnorte6Page } from './lanao-delnorte6.page';

describe('LanaoDelnorte6Page', () => {
  let component: LanaoDelnorte6Page;
  let fixture: ComponentFixture<LanaoDelnorte6Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(LanaoDelnorte6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
