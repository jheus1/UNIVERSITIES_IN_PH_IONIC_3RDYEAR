import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SurigaoDelsur3Page } from './surigao-delsur3.page';

describe('SurigaoDelsur3Page', () => {
  let component: SurigaoDelsur3Page;
  let fixture: ComponentFixture<SurigaoDelsur3Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SurigaoDelsur3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
