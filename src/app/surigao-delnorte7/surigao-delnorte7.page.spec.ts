import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SurigaoDelnorte7Page } from './surigao-delnorte7.page';

describe('SurigaoDelnorte7Page', () => {
  let component: SurigaoDelnorte7Page;
  let fixture: ComponentFixture<SurigaoDelnorte7Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SurigaoDelnorte7Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
