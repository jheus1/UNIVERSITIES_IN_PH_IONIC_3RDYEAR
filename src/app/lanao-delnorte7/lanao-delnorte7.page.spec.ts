import { ComponentFixture, TestBed } from '@angular/core/testing';
import { LanaoDelnorte7Page } from './lanao-delnorte7.page';

describe('LanaoDelnorte7Page', () => {
  let component: LanaoDelnorte7Page;
  let fixture: ComponentFixture<LanaoDelnorte7Page>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(LanaoDelnorte7Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
